<div data-controller="activities">
  <div id="searchForm">
  </div><div class="container">
    <h1 class="text-center text-4xl">Edit Activity</h1>
    <%= link_to sanitize("&larr; Back to Trip"), trip_path(@trip.id), class: "btn btn-success" %>
    <div class="container">
      <%= render partial: "activities", locals: {
                                        path: trip_daily_itinerary_activity_path(params[:trip_id], params[:daily_itinerary_id], params[:id]),
                                        method: :patch,
                                        activity_name: @activity.name,
                                        activity_description: @activity.description,
                                        activity_expenses: @activity.expenses,
                                        activity_address: @activity.address,
                                        button: "Update Activity"
      } %>
    </div>
  </div>
  </div>
