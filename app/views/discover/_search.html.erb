  <%= form_with url: discover_index_path, method: :get do |form| %>
    <%= form.label :search, "Search for a place to discover:" %>
    <%= text_field_tag(:search, params[:search]) %>
    <br />
    <%= form.submit "Search" %>
  <% end %>
