require "rails_helper"

RSpec.describe "Discover Index", type: :feature do
  context "a user searches for a location" do
    it "displays the results with clickable links" do
      within "#-#{@.id}" do 
      
      end 
      expect(page).to have_content
    end
  end
end
